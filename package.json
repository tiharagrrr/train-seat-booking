{
  "name": "railway-seat-segments",
  "private": true,
  "workspaces": ["api", "web"],
  "scripts": {
    "dev": "concurrently -n api,worker,web -c blue,magenta,green \"npm run dev -w api\" \"npm run dev:worker -w api\" \"npm run dev -w web\"",
    "test": "npm test -w api",
    "build": "npm run build -w api && npm run build -w web"
  },
  "devDependencies": { "concurrently": "^9.1.2" }
}
